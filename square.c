#include<stdio.h>
main()
{
    int side;
    printf("enter the side\n");
    scanf("%d",&side);
    A=side*side;
    {
        printf("area of square is %d m*m", A);
        return 0;
    }
}